{
  "name": "kuasbackend",
  "version": "1.0.0",
  "description": "deploy react ",
  "main": "index.js",
  "engines": {
    "node": "14.17.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "heroku-postbuild": "cd unicorn-front && npm install && npm run build && npm run prod"
  },
  "author": "Mekdim",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2"
  }
}
