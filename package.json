{
  "name": "kuasbackend",
  "version": "1.0.0",
  "description": "deploy react ",
  "main": "index.js",
  "engines": {
    "node": "14.17.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "heroku-prebuild": "cd unicorn-front && npm install --dev",
    "heroku-postbuild": "cd unicorn-front && npm install && npm run build"
  },
  "author": "Mekdim",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2"
  }
}
